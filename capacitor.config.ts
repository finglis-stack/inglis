import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.inglisdominion',
  appName: 'Inglis Dominion',
  webDir: 'dist'
};

export default config;
